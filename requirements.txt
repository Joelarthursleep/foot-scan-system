# ========== WEB FRAMEWORK ==========
streamlit>=1.30.0

# ========== SCIENTIFIC COMPUTING ==========
# Versions with pre-built wheels for Python 3.11
numpy==1.26.4
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2

# ========== VISUALIZATION ==========
plotly>=5.18.0
matplotlib>=3.8.0

# ========== 3D MESH PROCESSING ==========
trimesh>=4.0.0
numpy-stl>=3.0.0  # Fast STL file I/O
open3d>=0.18.0  # Advanced 3D processing (optional, for future enhancements)

# ========== COMPUTER VISION ==========
opencv-python-headless>=4.9.0
Pillow>=10.0.0

# ========== DATABASE (Medical-Grade) ==========
sqlalchemy[asyncio]>=2.0.0  # Async ORM
asyncpg>=0.29.0  # PostgreSQL async driver
alembic>=1.13.0  # Database migrations
psycopg2-binary>=2.9.0  # PostgreSQL sync driver (for migrations)

# ========== SECURITY (DSPT/Cyber Essentials Plus) ==========
cryptography>=41.0.0  # AES-256-GCM encryption
argon2-cffi>=23.1.0  # Password hashing
PyJWT>=2.8.0  # JWT authentication
python-multipart>=0.0.6  # File upload handling

# ========== CACHING & QUEUE ==========
redis>=5.0.0  # Caching and session storage
celery>=5.3.0  # Async task queue (future)
hiredis>=2.2.0  # Fast Redis protocol parser

# ========== API FRAMEWORK (Medical-Grade REST API) ==========
fastapi>=0.109.0  # Modern async API framework
uvicorn[standard]>=0.27.0  # ASGI server
pydantic>=2.5.0  # Data validation
pydantic-settings>=2.1.0  # Settings management
python-jose[cryptography]>=3.3.0  # JWT handling

# ========== VALIDATION & SCHEMAS ==========
email-validator>=2.1.0  # Email validation
phonenumbers>=8.13.0  # Phone number validation (for NHS contact details)

# ========== INTEROPERABILITY (Future) ==========
# fhir.resources>=7.0.0  # FHIR R4 data models (uncomment when needed)
# hl7apy>=1.3.0  # HL7 v2.x parsing (uncomment when needed)

# ========== MONITORING & LOGGING ==========
structlog>=24.1.0  # Structured logging
python-json-logger>=2.0.0  # JSON log formatting
sentry-sdk>=1.40.0  # Error tracking (optional)

# ========== TESTING (Development) ==========
pytest>=7.4.0
pytest-asyncio>=0.23.0
pytest-cov>=4.1.0
httpx>=0.26.0  # Async HTTP client for testing
faker>=22.0.0  # Generate test data

# ========== FILE PROCESSING ==========
h5py>=3.10.0
openpyxl>=3.1.0  # Excel files (for hazard log, validation data)

# ========== UTILITIES ==========
tqdm>=4.66.0
python-dotenv>=1.0.0
requests>=2.31.0
pyyaml>=6.0.0  # Configuration files
click>=8.1.0  # CLI tools

# ========== CLINICAL & MEDICAL ==========
# scipy includes statistical functions for clinical validation
# scikit-learn includes ML models for diagnostic engine
